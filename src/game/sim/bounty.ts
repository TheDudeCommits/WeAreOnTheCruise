/**
 * Named bounty captains (FOES-owned): rare named elites with two affixes, heavier hulls, a title on their nameplate,
 * a director-event banner when they sail in, a big bounty and a captain's chest when they sink.
 */
import type { SimContext } from './context';

export function updateNamedCaptains(_c: SimContext): void {}

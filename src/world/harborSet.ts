/**
 * Menu harbour set (WORLD-owned): the hand-placed harbour town, lighthouse and cliffs framing the title/harbour
 * showcase ship at the origin.
 */
import type { WorldFeature } from './features';

export function harborSetFeatures(): readonly WorldFeature[] { return []; }

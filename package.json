{
  "name": "we-are-on-the-cruise",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "vitest run",
    "capture": "node scripts/capture.mjs",
    "capture:ships": "node scripts/capture-ships.mjs",
    "profile": "node scripts/profile.mjs"
  },
  "dependencies": {
    "three": "latest"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "@types/three": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
